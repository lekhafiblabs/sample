<p id="notice"><%= notice %></p>
<div class="slots">

<h1 class="head">Slots</h1>

<!--<table>
  <thead>
    <tr>
      <th  class="movtext">Movie Slots</th>
      <th colspan="3"></th>
    </tr>
  </thead>
<br>


 <tbody>
    <% @slots.each do |slot| %>
      <tr>
     
          <td class="movtext"><%= slot.timings %></td>
      
        <td class="movcrud"><%= link_to 'Book', book_user_path(:slot_id => slot.id,:user_id => @user_id, :mov_id=>@mov_id) %></td>
      

            <% if current_user && current_user.admin %>
            <td class="movcrud" ><%= link_to 'Edit', edit_slot_path(slot) %>
        <td class="movcrud"><%= link_to 'Destroy', slot, method: :delete, data: { confirm: 'Are you sure?' } %></td> 
     <%end %>
      </tr>
    <% end %>
  </tbody>
</table>-->

<br> 


<div class="movcrud">
<%= link_to 'New Slot', new_slot_path %>
<%= link_to 'Home', home_index_path %>
</div>
</div>
